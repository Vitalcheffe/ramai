# Rami-AI dependencies
# Core (CLI / dev)
numpy>=2.0
opencv-python-headless>=4.10
pytest>=8.0

# Vision (Colab only — heavy)
# ultralytics>=8.2
# torch>=2.0
# ipywidgets>=8.0

# Optional: training visualizations
matplotlib>=3.8
